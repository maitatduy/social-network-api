export enum UserVerifyStatus {
    Unverified,
    Verified,
    Banned,
}

export enum TokenType {
    AccessToken,
    RefreshToken,
    EmailRefreshToken,
    EmailVerifyToken,
    ForgotPasswordToken,
}

export enum MediaType {
    Image = 0,
    Video = 1,
}
